Step 0 — tiny pre-existing fix: In server/routes.ts, the /api/jobs/:id/apply route has a TypeScript error because Express 5 widens req.params.id. Change parseInt(req.params.id) to parseInt(req.params.id as string) on that route only, so npm run check is clean. Don't change its behavior.

Step 1 — install the sanitizer: Add dompurify and @types/dompurify. Create a small helper client/src/lib/sanitizeHtml.ts that wraps DOMPurify and allows only these tags: p, h2, h3, h4, ul, ol, li, blockquote, aside, strong, em, b, i, a, img, figure, figcaption, br, span and attributes: class, href, src, alt, target, rel. Strip everything else (especially script, style, event handlers like onclick). For <a>, force rel="noopener noreferrer" on links that have target="_blank".

Step 2 — a shared mapper: Create client/src/lib/posts.ts with a Post type (matching the API fields) and a helper localizePost(post, lang) returning { slug, title, excerpt, body, category, coverImage, createdAt } picking the EN or FR fields based on lang ("en" | "fr").

Step 3 — InsightsPage (index): Fetch GET /api/posts with react-query. Render the SAME card layout/markup the page uses today, but driven by the fetched posts (mapped through localizePost). Use each post's coverImage for the card image (show a tasteful fallback/placeholder if coverImage is null). Keep the page's header/intro/footer chrome exactly as it is. Handle loading (skeleton or the existing loading treatment) and empty state (a simple "No insights yet" message).

Step 4 — ArticlePage (detail): Fetch GET /api/posts/:slug. On 404, show the existing not-found / fallback treatment. Render title, category, cover image, and the body. The body is an HTML string — render it via dangerouslySetInnerHTML only after passing it through sanitizeHtml, inside a typography wrapper (use the existing @tailwindcss/typography prose styling or whatever the page already uses) so headings, quotes, lists, and the .lead/.callout/.eyebrow classes look consistent with the current design. Match the current article visual style as closely as possible.